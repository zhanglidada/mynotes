2.2.3 补码编码

补码编码中的最高有效位是符号位

符号位的权重最高，是$-2^{w-1}$, 此时符号位可以理解为模。

2.2.4
有符号数和无符号数之间的转换

因为有符号数在机器中一般都是使用补码的方式表示，所以有符号数字在和无符号数之间进行转换时，需要注意最高位的权重问题。

在有符号数为正数时，其补码和源码一样，且最高位为0，所以此时可以直接转换为无符号数字。

有符号数为负数时，因为其采用补码方式表示，其最高位为1，权重为$-2^{w-1}$,此时其二进制位转为无符号数时，权重就从$-2^{w-1}$转为$2^{w-1}$，二者一进一出正好相差$2^w$
